# Contributing

Read `AGENTS.md` and run `scripts/agent-resume.ps1` before changing code. Keep exactly one primary task active, add tests and Javadocs with production changes, and record architecture changes as ADRs.

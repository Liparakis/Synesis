# Physical generated-launcher onboarding evidence

## Status

`NOT CLAIMED` — requires two physical computers and an operator-run generated
launcher scenario.

## Required record

Run the bounded normal-operation scenario from
[`docs/demo/FIRST_DEMO.md`](../demo/FIRST_DEMO.md) using the generated
`synesis.bat` launcher on both machines. Record only sanitized machine labels,
operating systems, Java versions, topology/address classes, candidate counts,
redacted pair ID, expected/authenticated node IDs, stable statuses, numeric
exits, close reason, cleanup, and exact command/date.

Do not record private keys, profile or descriptor contents, passwords, full
personal addresses, usernames, access tokens, or absolute personal paths. The
existing `PHYSICAL-DEMO-2026-07-20.md` proves only the diagnostic `DemoCli`
path and must not be reused as launcher evidence.

# CP-0480.md

- Checkpoint ID: CP-0480
- Task ID: SYN-039
- Status: ACTIVE
- Date: 2026-08-24
- Branch: master
- Push status: not pushed

## Completed slice

CP-0480 reproduced the peer/WorkGroup decision boundary with fresh
claim-bearing and ordinary two-agent fixtures. Both runs converged on one
active WorkGroup; the backend did not split unrelated groups under the normal
default-group path. The ordinary run exposed a real projection defect:
`REVIEW_ADMISSION_REQUIRED` carried the exact `work_group_join` payload inside
`reviewActions`, while the executable workflow advertised
`request_coordination` with empty arguments. The selected kind and payload are
now promoted to the response root and copied into exact executable arguments.

Regression coverage in `McpSyn039SliceTest` asserts the exact projected
`request_coordination` kind, WorkGroup ID, and intent ID. Focused MCP and
workspace tests passed, and the current bundled MCP was rebuilt successfully.

Evidence: `docs/evidence/syn039-unattended-todo-cp0480-convergence-projection-2026-08-24.md`.

## Verification

- Focused MCP/workspace tests: PASS.
- Deferred/fixture validators: PASS.
- `git diff --check`: PASS.
- Bootstrap `go test ./...` and `go vet ./...`: three existing migration-test failures (`update migrations not prepared`); other bootstrap packages passed.
- Doctor: DEGRADED with existing warnings.
- Full root `check`: not completed; known Git subprocess stall remains separately classified.

## Remaining work / exact continuation

Run a fresh post-fix bounded diagnostic and completely ordinary two-agent Todo
acceptance with the rebuilt current MCP. Capture every projection/action and
preserve the first lifecycle blocker after exact REVIEW admission. Do not
broaden cleanup, integration, Doctor, or ownership behavior; do not push or
create SYN-040.

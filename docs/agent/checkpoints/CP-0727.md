# Checkpoint CP-0727 — SYN-049 runs #70–#74

Date: 2026-09-06

## Result

SYN-049 remains **PARTIAL / ACTIVE**. Fresh runs #70 and #71 proved the
generation-1 managed/capability/wake/terminal lifecycle but failed independent
integrated compilation on A-produced contract shapes. Fresh run #72 exposed a
disposable runner flag bug, #73 exposed a second disposable runner ordering bug,
and #74 stopped on the first material runtime failure: A generation-1 AppServer
`process_exit` during the real owner turn.

## Evidence

See `docs/evidence/syn049-fresh-unattended-two-worker-2026-09-06-runs70-74.md`.
The source checkout is clean at `c9733fd9e35a97332465f7b5326fe4bb96aba075`.
No production source changed. The JDK25 process-local AF_UNIX workaround and
locked artifact hashes remained exact.

## Exact continuation

Investigate the run-#74 AppServer `process_exit` read-only, including the
run-scoped lifecycle evidence and provider process tree. Do not repair durable
state, reuse a prior target, patch production speculatively, invoke replacement
or A2, or claim SYN-049 completion.

# CP-0458.md

- Checkpoint ID: CP-0458.md
- Creation time: 2026-08-05T13:12:06.2405915+03:00
- Task: SYN-038 durable project-command extension
- Task status at implementation commit: complete
- Branch: master
- Implementation commit: `ad9fdd8addc9f71e806dfb2da5b5d78f050f87ac`
- Prior evidence: CP-0457 (preserved unchanged)
- Previous lifecycle history: preserved at the earlier SYN-038 commit,
  CP-0447/CP-0448, ADR-0043, and the existing acceptance evidence.

## Closure record

The durable project-command extension is complete. The implementation keeps
the existing ten-tool MCP catalog, App Server ownership, command executor,
lease/workspace protections, cleanup/repair entry points, and independent
Codex interruption semantics. No daemon, listener, event bus, provider
abstraction, process owner, approval operation, or SYN-039 was added.

The shared Git subprocess runner now closes stdin, safely drains and bounds
output, disables prompts/editors/signing/hooks/fsmonitor, applies a strict
deadline, and terminates descendants on timeout. The durable admission
regression was narrowed to `PARTICIPANT_NOT_FOUND` for a valid no-claims
session; `heartbeatIfPresent` ignores only that absence. Lease admission still
renews once, captures the exact post-renew snapshot, reacquires and revalidates,
and does not launch or persist STARTING after unexpected change.

## Verification

All required closure checks passed:

- full `check`;
- focused `McpServerTest` (32/32);
- focused durable namespace, process-runner, MCP framing, and supporting
  regressions;
- deferred and fixture validators;
- bootstrap Go tests and vet;
- strict Javadocs and format checks;
- doctor; and
- `git diff --check`.

The final closure documentation is committed separately after this checkpoint.

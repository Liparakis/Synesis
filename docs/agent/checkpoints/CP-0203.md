# CP-0203.md

- Checkpoint ID: CP-0203.md
- Creation time: 2026-07-26T02:00:00+03:00
- Active task: SYN-014E
- Active-task status: ACTIVE
- Branch: master
- Base commit resolved: `b61b0dc52278e137d4f7c4275e7d62143ffae50a`
- Reported commits: `e278ea735318c6dbf84d8bcff3435034335c2322`, `fe8b3e3`
- Base discrepancy: resolved; `fe8b3e3` resolves to `fe8b3e301662114acef6fc0c6457e6b681a2fc60`, a descendant of `e278ea7`, followed by `a642c9d` and `b61b0dc` documentation commits.
- Working-tree status before implementation: clean.
- Completed work: preserved integral JSON numbers in `ProviderJson`, added MCP protocol-version negotiation for supported versions, added opt-in trace-file diagnostics outside stdout, and added regression tests.
- Installed active Version B: `0.1.0-dev-local-5c6` through the real bootstrap installer; active pointer and manifest validation passed.
- Direct payload evidence: initialize negotiated `2025-06-18`; `tools/list` returned exactly 11; stdout contained only protocol JSON; stderr remained separate.
- Stable/`.cmd` evidence: the stable pointer `.cmd` path resolved Version B and returned the same 11-tool response with inherited stdio and preserved working directory.
- Real Codex evidence: Codex `0.140.0` sent `initialize` with `protocolVersion=2025-06-18`, received a valid response with integer `id=0`, sent `notifications/initialized`, sent `tools/list`, and received all 11 tool definitions. Codex then attempted `synesis.ensure_session`, `synesis.read_file`, and `synesis.apply_patch` in separate runs; the noninteractive runner cancelled those calls. No successful tool execution is claimed.
- Known-good control evidence: disposable local control MCP handshook manually and was exposed/attempted by Codex in the prior bounded acceptance; configuration was restored by compare-and-set hash.
- Primary classification: `SYNESIS_MCP_INITIALIZE_RESPONSE_FAILURE` (invalid integral JSON-RPC identifier serialization caused Codex to reject the response; protocol-version negotiation was corrected in the same slice).
- Verification commands: `:coordination:check :workspace:check :cli:check :mcp:check check --no-daemon`; `go test ./...`; `go vet ./...`; all passed.
- Current failures: Codex noninteractive MCP tool calls are cancelled by the runner; Antigravity remains `REAL_DISCOVERY_UNPROVEN`.
- Exact continuation: run `powershell -ExecutionPolicy Bypass -File scripts/agent-resume.ps1`, review this checkpoint, and either close SYN-014E or record the remaining Codex execution limitation.

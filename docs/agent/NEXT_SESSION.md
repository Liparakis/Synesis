# Next Session

- Active task: SYN-014E ACTIVE at Slice 5C.6 following CP-0206
- Repository branch: master
- Last checkpoint: CP-0206
- Last passing command: `:coordination:check :workspace:check :cli:check :mcp:check check`, `go test ./...`, and `go vet ./...`.
- Immediate next command: `powershell -ExecutionPolicy Bypass -File scripts/agent-resume.ps1`
- Exact next code action: Report SIGNING_BLOCKED for Slice 5C.6; do not retry activation without the established trusted signing key.
- Unresolved limitations: The current API has no approval-capable interactive prompt driver; `codex exec` remains noninteractive. The local Version B was activated from an unsigned development-only manifest, so signed-update activation is not claimed. Five dirty legacy worktrees are preserved for operator review. Actual worktree cleanup uses non-forced `git worktree remove`. Unregistered orphan resources are quarantined atomically without purge. Session abandonment and task cancellation preserve worktrees non-destructively for operator review. Provider configuration files remain diagnostic-only. Event log and snapshot failures require human review.
- Facts that must not be forgotten: Handles format must be `req_<random_token>` with at least 96 bits entropy. MCP responses must remain concise and must not leak internal IDs, worktree paths, or event IDs. Exactly 11 MCP tools are currently registered in `tools/list`. `synesis doctor` is strictly read-only by construction. Repair administrative state is stored under `%LOCALAPPDATA%\Synesis\workspaces\<project-id>\admin\repair-plans\<plan-id>.json`. Repair execution lock is at `%LOCALAPPDATA%\Synesis\workspaces\<project-id>\admin\repair-execution.lock`. Repair execution journals are at `%LOCALAPPDATA%\Synesis\workspaces\<project-id>\admin\repair-executions\<execution-id>.jsonl`. Pre-mutation backups are stored under `%LOCALAPPDATA%\Synesis\workspaces\<project-id>\admin\repair-backups\<execution-id>\`.
- Slice 5 boundary: installed payloads are immutable and retained; activation is a validated atomic pointer replacement; update execution requires a prepared plan; provider/project migration is compare-and-set and identity-preserving; no process termination, old-version deletion, remote polling, or MCP tool changes.
- Slice 5C.3 evidence: active Version B was `0.1.0-dev-local-5c7`; Codex TOML remains the preserved `.cmd` entry; direct and `.cmd` handshakes negotiate `2025-06-18`, preserve integer JSON-RPC IDs, and return exactly 11 tools.
- Slice 5C.4 evidence: fixture safety backup is `C:\Users\Liparakis\Desktop\SynesisTestProject-Safety-20260726-034531`; baseline branch is `synesis-codex-acceptance`; reconcile/cleanup plans executed with zero forced actions.
- Slice 5C.5 evidence: source commits `1a97373` and `0df0ff7`; installed Version B `0.1.0-dev-local-5c9`; one persistent MCP wire sequence returned exactly 11 tools, ensured an isolated session, read the GreetingService with a content hash, applied a protected patch, ran `run-tests.cmd` successfully, and returned a ready next action; control checkout remained unchanged. Disposable tests cover exact connection resolution and clean/dirty stale generation behavior. Desktop interactive Codex tool execution remains unclaimed because no approval-capable prompt driver is exposed in this API.
- Slice 5C.6 evidence: actual HEAD `8d5447b`; production fixes are ancestors; the `0.1.0-dev-local-5c10` release-mode candidate was built with SHA-256 inventory, but the repository signer rejected the absent `SYNESIS_MANIFEST_PRIVATE_KEY_B64` key and the updater rejected the unsigned manifest before prepare. Gradle checks, Go tests, and Go vet passed. Active installed version remains `0.1.0-dev-local-5c9`; signed activation and real Codex attribution are blocked.
